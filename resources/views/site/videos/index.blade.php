@extends('site.main')@section('meta_tags')    <title>{{ trans('site.videos.index') }}</title>    <link rel="canonical" href="{{ route('site.videos.index') }}">    <meta name="keywords" content="{{ trans('site.videos.keywords') }}">    <meta name="description" content="{{ trans('site.videos.description') }}">    <meta property="og:title" content="{{ trans('site.videos.index') }}"/>    <meta property="og:description" content="{{ trans('site.videos.description') }}"/>@stop@section('content')    <section class="page-title theme-overlay overlay-black" style="background-image: url('/images/parallax/image-3.jpg');">        <div class="auto-container">            <h1>{{ trans('site.videos.index') }}</h1>            {!! Breadcrumbs::render('site.videos.list') !!}        </div>    </section>    <section class="team-section pt-90 pb-60">        <div class="container">            <ul class="row clearfix team-area-hvr">                @foreach($items as $item)                    <li class="col-md-6 col-sm-6 col-xs-12">                        <article class="team-area">                            <div class="row clearfix">                                <div class="col-lg-6 col-md-12">                                    <div class="content">                                        <div class="title-box">                                            <h4>{{ $item->title }}</h4>                                            <p>{{ $item->jalali_published_at->format('d F Y') }}</p>                                        </div>                                    </div>                                </div>                                <figure class="post-header">                                    <video controls="controls" style="height: 365px" class="col-xs-12">                                        <source src="{{ $item->video_url }}" type="video/mp4" />                                    </video>                                </figure>                            </div>                        </article>                    </li>                @endforeach            </ul>            <div class="pager-outer text-center">                {!! $items->links() !!}            </div>        </div>    </section>@stop@section('stylesheets')@stop@section('scripts')    <script src="{{ URL::to('/') }}/assets/js/page-in.js"></script>@stop