@extends('site.main')@section('meta_tags')    <title>{{ trans('site.blog.index') }} | {{ getSetting('site_title') }}</title>    <link rel="canonical" href="{{ route('site.blog.index') }}">    <meta name="keywords" content="{{ trans('site.news.keywords') }}">    <meta name="description" content="{{ trans('site.news.description') }}">    <meta property="og:title" content="{{ trans('site.blog.index') }}"/>    <meta property="og:description" content="{{ trans('site.news.description') }}"/>@stop@section('content')    <section id="sp-main-body">        <!-- Page Content -->        <div class="container">            <div class="row">                <div class="col-lg-12" style="margin-bottom: 50px">                    <div class="row" style="margin-top: 50px; margin-bottom: 50px">                        @foreach($items as $item)                            <div class="col-lg-4 col-md-6 mb-6" style="height: 400px">                                <div class="card h-100">                                    <a href="{{ $item->link }}"><img class="card-img-top" src="{{ $item->image_link }}" width="700" height="400" alt="{{ $item->title }}"></a>                                    <div class="card-body">                                        <h4 class="card-title">                                            <a href="{{ $item->link }}">{{ $item->title }}</a>                                        </h4>                                        <p class="card-text"> {!! $item->excerpt !!}</p>                                    </div>                                </div>                            </div>                        @endforeach                    </div>                    {!! $items->links() !!}                </div>            </div>        </div>    </section>@stop@section('stylesheets')@stop@section('scripts')@stop